node_modules/
.next/
dist/
build/
coverage/

.env
.env.*
!.env.example

*.db
*.db-shm
*.db-wal

.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# generated/debug artifacts — not source
.test-artifacts/
playwright-report/
test-results/
*.tsbuildinfo
next-env.d.ts

# generated by the /graphify skill — a regenerable local analysis cache, not project source
graphify-out/

# Gmail OAuth tokens (Checkpoint 17) — local-only, never committed
.gmail-token.json

# User preferences (Checkpoint 23) — local-only persistent config, never
# committed, and deliberately never the same file as OAuth tokens above.
.jarvis-preferences.json

# Scheduled reminders (Checkpoint 29) — local-only persistent state, never
# committed, and deliberately its own separate file — never the same file
# as OAuth tokens or preferences above.
.jarvis-reminders.json
